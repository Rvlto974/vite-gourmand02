# 🚀 API REST - Vite & Gourmand

## Endpoints

- GET /api/plats → 13 plats
- GET /api/menus → 6 menus
- GET /api/commandes → 39 commandes
- GET /api/avis → Avis validés
- GET /api/utilisateurs/:id → Info user

## Tests
curl http://localhost:8080/api/plats
